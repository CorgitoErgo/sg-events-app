# Copy to .env.local (gitignored). A phone can't reach "localhost" on your PC: use the PC's
# LAN address and start the API with: uvicorn app.main:app --host 0.0.0.0 --port 9000
EXPO_PUBLIC_API_URL=http://192.168.0.4:9000
